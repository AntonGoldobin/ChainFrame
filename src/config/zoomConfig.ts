export interface IConfig {
  frame: {
    defaultWidth: number;
    defaultHeight: number;
  };
}

export const zoomConfig: IConfig = {
  frame: {
    defaultWidth: 1000,
    defaultHeight: 1000,
  },
};
